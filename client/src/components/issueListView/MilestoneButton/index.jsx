import React, { useState, useRef } from 'react';
import './style.scss';

const input = () => {
  return (
    <button id = "Milestone-button" type = "button">Milestone</button>
  );
};

export default input;
