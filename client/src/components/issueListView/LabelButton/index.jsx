import React, { useState, useRef } from 'react';
import './style.scss';

const input = () => {
  return (
    <button id = "Label-button" type = "button">Label</button>
  );
};

export default input;
