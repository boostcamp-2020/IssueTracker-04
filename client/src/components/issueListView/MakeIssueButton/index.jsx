import React, { useState, useRef } from 'react';
import './style.scss';


const input = () => {
  return (
    <button id = "MakeIssue-button" type = "button">New Issue</button>
  );
};

export default input;
