import React, { useState, useRef } from 'react';
import './style.scss';

const labels = () => {

  return (
    <div className="create-register">
      <summary>
        Labels
      </summary>
      <span>None yet</span>
    </div>
  );
};

export default labels;
