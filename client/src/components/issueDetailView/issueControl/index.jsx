import React, { useState, useRef } from 'react';
import './style.scss';

const issueControl = () => {

  return (
    <button className="issue-control">
      issue
    </button>
  );
};

export default issueControl;
