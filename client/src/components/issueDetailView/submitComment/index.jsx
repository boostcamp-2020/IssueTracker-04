import React, { useState, useRef } from 'react';
import './style.scss';

const submitComment = () => {

  return (
    <button className="submit-comment">
      Comment
    </button>
  );
};

export default submitComment;
