import React, { useState, useRef } from 'react';
import './style.scss';

const cancel = () => {

  return (
    <span className="create-cancel">
        Cancel
    </span>
  );
};

export default cancel;
