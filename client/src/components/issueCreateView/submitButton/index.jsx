import React, { useState, useRef } from 'react';
import './style.scss';

const submitButton = () => {

  return (
    <button className="submit-button">
        Submit new issue
    </button>
  );
};

export default submitButton;
